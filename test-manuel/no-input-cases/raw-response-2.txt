{"execution_profile":{"intent_summary":"Vérifier que toutes les images présentes sur la page de login sont correctement chargées et ne sont pas cassées.","coverage_type":"generic_ui","preconditions":["La page de login doit être accessible à l'URL de base.","La page doit être complètement chargée avant l'inspection des balises image.","Aucun identifiant ni saisie utilisateur n'est requis pour ce test."],"required_inputs":[],"expected_observations":["Chaque balise img présente sur la page possède un attribut src non vide.","Chaque image est marquée comme complètement chargée via complete === true.","Chaque image a une largeur naturelle strictement supérieure à 0, indiquant l'absence d'image cassée."]},"python_script_body":"await page.wait_for_load_state('domcontentloaded')\nimages = page.locator('img')\ncount = await images.count()\nif count > 0:\n    await expect(images.first()).to_be_visible()\nresults = await page.evaluate(\"\"\"\n() => Array.from(document.images).map((img, index) => ({\n    index,\n    src: img.getAttribute('src') || '',\n    complete: img.complete,\n    naturalWidth: img.naturalWidth\n}))\n\"\"\")\nfor image in results:\n    await expect(page.locator(f'img').nth(image['index'])).to_have_attribute('src', r'.+')\n    await expect(page.locator(f'img').nth(image['index'])).to_have_js_property('complete', True)\n    await expect(page.locator(f'img').nth(image['index'])).not_to_have_js_property('naturalWidth', 0)","human_readable_steps":["Attendre que la page de login soit chargée.","Repérer toutes les balises image présentes sur la page.","Vérifier que la première image est visible lorsqu'au moins une image est présente.","Inspecter les propriétés DOM de chaque image pour récupérer src, complete et naturalWidth.","Vérifier pour chaque image que l'attribut src n'est pas vide.","Vérifier pour chaque image que la propriété complete vaut true.","Vérifier pour chaque image que naturalWidth est différent de 0 pour confirmer qu'elle est bien chargée."]}